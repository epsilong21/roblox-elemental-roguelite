# Changelog

## Unreleased

- Entorno de colaboración ChatGPT ↔ Claude definido.
- Arquitectura documental inicial creada.
- ENV-001 — Configuración del workspace Rojo: `default.project.json` mapea `src/` al árbol de Roblox Studio aprobado (`ReplicatedStorage/Roguelite/{Shared,Config}`, `ServerScriptService/RogueliteServer`, `StarterPlayer/StarterPlayerScripts/RogueliteClient`). `rojo build` validado y sincronización real Rojo → Roblox Studio confirmada.
- M1-001 — Movimiento: **DONE**. Locomoción base con `Humanoid` estándar (`MovementConfig`, `MovementService`, `MovementBootstrap`). WalkSpeed = 16 (aprobado como valor actual, no definitivo), salto y AutoJump desactivados server-side, sin RemoteEvents de movimiento. QA completo PASS (spawn, cardinal, diagonal, stop, jump disabled, rotation, respawn, multiplayer, mobile, output). Deuda registrada: validaciones server-side contra movimiento anómalo antes de beta.
- M1-002 — Cámara: implementada cámara base sobre el sistema nativo de Roblox (`CameraConfig`, `CameraController`). Tercera persona, distancia fija 18 studs, CameraOffset Y=2.5, FOV=70, Follow en PC/móvil, Invisicam, sin cámara custom ni targeting. Pendiente de QA — no marcar DONE todavía.
- M1-002 — Fix QA-CAM-BLOCKER-001: `CameraMode`/zoom/`Dev*` de cámara movidos de escritura runtime (rechazada por Roblox por permisos) a defaults estáticos de `StarterPlayer` vía `$properties` en `default.project.json`. `CameraConfig.lua` reducido a `CameraOffset`/`FieldOfView`. Sigue pendiente de QA.
- M1-002 — Diagnóstico: los defaults de `StarterPlayer` no se sincronizan vía Rojo live sync (limitación documentada de Rojo: propiedades "Unscriptable" que el plugin de Studio no puede escribir en caliente). `rojo build` sí los serializa correctamente (verificado). Añadido `$className: "StarterPlayer"` explícito. Workaround: generar y abrir `Roguelite.rbxl` en vez de depender de live sync para esas propiedades. Sigue pendiente de QA.
- M1-002 — Cámara: **DONE**. QA completo PASS (spawn, distance, manual rotation, follow, movement compatibility, occlusion, respawn, multiplayer, mobile, output). Game feel aprobado para prototipo (distancia 18, offset Y=2.5, follow: Bien; visibilidad general: Aceptable — a revisar cuando exista una sala de combate real con enemigos). `QA-CAM-BLOCKER-001` resuelto y arquitectura confirmada (defaults estáticos de `StarterPlayer` vía Rojo; limitación de live sync para propiedades Unscriptable documentada como proceso, no como bug).
